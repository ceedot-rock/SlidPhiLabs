# Sealed `/pcc` diner receipts

Claimable strip fields only. **No Fast chrome.** Public product name is **PCC**.

- [`pcc-diner-dial-a-mozilla.json`](pcc-diner-dial-a-mozilla.json) — Dial A Scout · mozilla · Out **16,708,129** · vs zstd-9 **−27,834** (−28k class) · Every byte back = DECODE_OK

Legacy paths `/receipts/gc-diner-dialA-mozilla.json` and `/receipts/pcc-diner-dialA-mozilla.json` **301** to the canonical file (no GC filenames in the tree).
